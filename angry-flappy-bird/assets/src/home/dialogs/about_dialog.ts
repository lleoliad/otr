const {ccclass, property} = cc._decorator;

@ccclass
export default class about_dialog extends cc.Component {
    
    // LIFE-CYCLE CALLBACKS:

    // onLoad () {}

    start () {

    }

    // update (dt) {}
}
